import { GaleriaPipePipe } from './galeria-pipe.pipe';

describe('GaleriaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GaleriaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
